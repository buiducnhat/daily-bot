{
  "name": "@daily-bot/auth",
  "exports": {
    ".": {
      "default": "./src/index.ts"
    },
    "./*": {
      "default": "./src/*.ts"
    }
  },
  "type": "module",
  "scripts": {
    "generate": "bunx @better-auth/cli generate --config src/index.ts --output ../db/src/schema/auth.ts"
  },
  "devDependencies": {
    "@daily-bot/config": "workspace:*"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "better-auth": "catalog:",
    "dotenv": "catalog:",
    "zod": "catalog:",
    "@daily-bot/db": "workspace:*",
    "@daily-bot/env": "workspace:*"
  }
}
